import styled from 'styled-components';

export const ChangeItem = styled.div`

    .change-item {
        background: none;
        border: 0;
        color: var(--theme-three);
        padding: 15px 0;    
    }
`;